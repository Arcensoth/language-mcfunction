---
name: mcfunction
scopeName: source.mcfunction
fileTypes:
  - mcfunction

names:
  # naming convention: <root_scope>._.<name>.<language>

  whitespace: meta._.whitespace.mcfunction
  root_command: keyword.control._.root_command.mcfunction
  comment: comment._.comment.mcfunction

  command_token.integer: constant.numeric._.integer.command_token.mcfunction
  command_token.unknown: string.unquoted._.unknown.command_token.mcfunction

variables:
  # Space
  S: '[ \t]*'

  # Line Left
  LL: '(?:\r\n|\n|^)'

  # Line Left with Space
  LLS: "{{LL}}{{S}}"

  # Line Right
  LR: '(?=\r\n|\n|$)'

  # Token Terminator
  TT: '(?=\s|$)'

  literal: "[a-z][a-z0-9_]*"
  integer: '\-?\d+'

loadPatterns:
  root: root
  command_tokens: command_tokens

patterns:
  - include: "#root"
